import { SalePercentPipe } from './sale-percent.pipe';

describe('SalePercentPipe', () => {
  it('create an instance', () => {
    const pipe = new SalePercentPipe();
    expect(pipe).toBeTruthy();
  });
});
